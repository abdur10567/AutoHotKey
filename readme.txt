Each file is a hotkey map/remap to Abdur's prefered keys. 
Each script it set to launch on startup. ctrl+shift+q will exit all scripts.
To add/remove scripts from startup, press windows+r and enter shell:common startup.
paste script shortcuts inside the folder.
